version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile-dev
    volumes:
      - gopath-data:/go
    ports:
      - "8000:8000"
    environment:
      PORT: 8000
      GCP_PROJECT: test
      SENTRY_DSN: ${SENTRY_DSN}
      SENTRY_ENVIRONMENT: development
      SENTRY_DEBUG: "true"

volumes:
  gopath-data:
    driver: local
